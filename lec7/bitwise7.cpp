#include <iostream>
#include <cmath>
using namespace std;

int main()
{
    // the most signitdicat bits is used for -ve and +ve sing
    // int a, b;
    // cout << "enter two number" << endl;
    // // cin >> a >> b;
    // int expo, num;
    // cout << (a & b) << endl;

    // bitwise << letd ship operater in a << b is
    // and a*2^b

    // bitwise >> right shift operater in a>>b id
    // ans = a/2^b;

    cout << sizeof(int) << endl;
    cout << sizeof(long long int) << endl;
    // cout << sizeof(short int) << endl;
    // cout << sizeof(unsigned int) << endl;
    // cout << sizeof(float) << endl;
    // cout << sizeof(char) << endl;

    // cout << "enter a number " << endl;
    // cin >> num;

    // for (expo = 1; expo <= 30; expo++)
    // {

    //     if (num == (int)pow(2, expo))
    //     {
    //         cout << "it is the powe of two " << endl;
    //         break;
    //     }
    // }

    return 0;
}